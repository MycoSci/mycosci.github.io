---
interface Props {
  label: string;
  icon?: string;
}
const { label, icon } = Astro.props;
---

<div class="tab-item card p-4 mb-2">
  <div class="text-xs font-mono text-lab-500 mb-2 flex items-center gap-2">
    {icon && <span>{icon}</span>}
    {label}
  </div>
  <slot />
</div>
